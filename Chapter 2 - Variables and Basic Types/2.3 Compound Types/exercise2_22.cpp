// Assuming p is a pointer to int, explain the following code:

int main()
{
    int *p;
    if (p) // if p points to an adress, do something
    if (*p) // if an int to which p points to is not 0, do something

    return 0;
}

